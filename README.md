┌─────────────────────────────────────────────────────────────────┐
│                           main.go                               │
│ 1. 加载配置 2. 初始化DB 3. 创建Engine 4. 注册Stage 5. 提交种子  │
│ 6. 启动Monitor 7. 等待信号 → 调用Engine.Stop                    │
└─────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────┐
│                          Engine                                 │
│ - 管理 stages: map[stageName]*StageInfo                        │
│ - 提供 SubmitTask(stage, task)                                 │
│ - 持有 BrowserPool                                             │
│ - Stop(): 取消全局Context，等待所有阶段停止，关闭BrowserPool    │
└─────────────────────────────────────────────────────────────────┘
│                              │
▼                              ▼
┌─────────────────────┐      ┌─────────────────────┐
│   Stage: catalog    │      │   Stage: detail    │
│ - WorkerPool        │      │ - WorkerPool        │
│ - Handler           │      │ - Handler           │
│ - Config            │      │ - Config            │
└─────────────────────┘      └─────────────────────┘
│                              │
▼                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       WorkerPool[T]                            │
│ - taskQueue: chan T                                            │
│ - workers: []goroutine                                         │
│ - Submit(task) → 放入队列                                       │
│ - Stop(timeout) → 等待或强制退出                                │
│ - Errors() chan error                                          │
│ - Activities() chan Activity                                   │
└─────────────────────────────────────────────────────────────────┘
│                              │
▼                              ▼
┌─────────────────────┐      ┌─────────────────────┐
│  Handler (Fetch...) │      │     Monitor         │
│ 1. 从 BrowserPool   │      │ 消费 Activities     │
│    获取浏览器        │      │ 更新统计信息        │
│ 2. 执行抓取/解析     │      │ 提供查询接口        │
│ 3. 生成新任务        │      └─────────────────────┘
│ 4. 调用 Engine      │
│    .SubmitTask()    │
└─────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────┐
│                      BrowserPool                                │
│ - browsers: chan *Browser                                      │
│ - commonOpts: []chromedp.ExecAllocatorOption                   │
│ - proxyManager: *ProxyManager                                  │
│ - Get() → 获取可用浏览器，支持健康检查                         │
│ - Put() → 归还                                                 │
│ - Close() → 关闭所有浏览器                                      │
└─────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────┐
│                     ProxyManager                                │
│ - 定时从API拉取代理列表，线程安全                               │
│ - Next() → 轮询返回代理                                         │
└─────────────────────────────────────────────────────────────────┘